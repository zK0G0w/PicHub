server:
  port: 8123
  servlet:
    context-path: /api
spring:
  application:
    name: pichub-backend
  # 数据库配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/pichub
    username: root
    password: 123456
